# Server Configuration (Backend)
PORT=
JWT_SECRET=
# production (Render) | development (local)
NODE_ENV=

# API base URL (Backend deployado no Render)
VITE_API_URL=

# URL(s) do frontend autorizadas a chamar a API (separadas por vírgula se houver mais de uma)
CORS_ORIGIN=

# Last.fm Configuration
VITE_LASTFM_API_KEY=

# PostgreSQL Configuration (Backend)
# Em nuvem (Render/Neon/Supabase), preencha DATABASE_URL — tem prioridade sobre as DB_* abaixo.
DATABASE_URL=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=
